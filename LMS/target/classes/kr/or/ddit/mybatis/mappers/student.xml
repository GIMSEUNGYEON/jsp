<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.student.dao.StudentDAO">
    <select id="selectScoreList">
        SELECT *
        FROM (
            SELECT CLASS.SUB_CD, CLASS.STD_NO, CLASS.CLS_SCORE, subject.sub_name
            FROM CLASS
            INNER JOIN SUBJECT ON CLASS.SUB_CD = SUBJECT.SUB_CD
        ) A
        WHERE INSTR(A.STD_NO, '20') > 0
    </select>
    <select id="selectClassList">
        SELECT SUB_CD, SUB_NAME, SUB_CREDIT
        FROM SUBJECT
    </select>
</mapper>	